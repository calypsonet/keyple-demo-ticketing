package org.calypsonet.keyple.demo.validation.domain.model

/**
 * Represents a terminal location (pure domain entity, no Android dependencies).
 */
data class Location(
    val id: Int,
    val name: String
)
